from flask import Flask, request

app = Flask(__name__)

# Vulnerable route
@app.route("/")
def index():
    name = request.args.get("name", "")
    # Intentionally rendering user input directly - VULNERABLE TO XSS
    return f"""
    <html>
        <head><title>XSS Demo</title></head>
        <body>
            <h1>Hello {name}</h1>
            <form method="GET">
                Enter your name: <input name="name">
                <input type="submit">
            </form>
        </body>
    </html>
    """

if __name__ == "__main__":
    app.run(debug=True)
